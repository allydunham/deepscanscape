# Compare new DMS dataset to the combined dataset
# TODO - Create compare
# TODO - Will need to re-run UMAP to do this (and store the trained model)

# and also potentially save the models of PCA and tSNE
#' Compare a new DMS dataset to the combined landscape
#'
#' @param x \link{deep_mutational_scan} to analyse
#'
#' @export
compare_dms <- function(x) {
  stop("Not implemented yet")
}
